{%- layout none -%}
{%- assign metafields = collection.metafields -%}
{
	"id": {{- collection.id -}},
	"handle": "{{- collection.handle -}}"
	{%- for field in metafields -%}
		,"{{- field[0] -}}": {{- field[1] | json -}}
		{%- unless forloop.last -%},{%- endunless -%}
	{%- endfor -%}
}